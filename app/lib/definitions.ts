export type AddTabs = {
  id: number;
  name: string;
};

export type Option = {
	id: number;
  name: string;
}

export type User = {
	id: string;
  name: string;
  phone: string;
  password: string;
}